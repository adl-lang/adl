module examples.pubsub1
{

import examples.pubsub.Subscribe;
import examples.pubsub.ChannelReq;

// A concrete message service, with strings for messagse and patterns

typedef string Message;
typedef string Pattern;
typedef Subscribe<Message,Pattern> MySubscribe;
typedef ChannelReq<Message,Pattern> MyChannelReq;

};