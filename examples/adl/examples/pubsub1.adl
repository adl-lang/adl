module examples.pubsub1
{

import examples.pubsub.*;

// A concrete message service, with strings for payloads and patterns

typedef String Payload;
typedef String Pattern;

typedef Message<Payload> MyMessage;
typedef Subscribe<Payload,Pattern> MySubscribe;
typedef ChannelReq<Payload,Pattern> MyChannelReq;

};